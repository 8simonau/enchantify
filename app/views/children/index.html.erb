<%= link_to "Retour", root_path, class: "btn btn-standard mb-3" %>

<div class="page">
  <div class="container">
    <div class= "children-grid">
      <% @children.each do |c| %>
        <%= link_to set_child_path(c) do %>
          <div class="child-card">
            <div class="child-image-border">
              <div class="child-image">
                <%= cl_image_tag(c.avatar.key,
                                width: 130,
                                height: 130,
                                crop: :fill,
                                gravity: :faces) %>
              </div>
            </div>
            <div class="child-name">
              <p><%= c.first_name %></p>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>
    <div class="buttons">
      <%= link_to "Ajouter un enfant", new_child_path, class: "btn btn-standard mb-3" %>
    </div>
  </div>
</div>
